      SUBROUTINE RDBACKCC     TAKE A STEP BACK IF CHARACTER IS NOT WHAT WAS ANTICIPATEDC      INCLUDE 'IODATA.CIN'      INCLUDE 'RDBUFF.CIN'C      ICOL = ICOL - 1      IF (ICOL .GE. 1) ITEM = KLINE(ICOL)      RETURN      END